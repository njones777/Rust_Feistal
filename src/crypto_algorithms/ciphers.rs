use bit_vec::BitVec;


rail_fence_encode(block: &BitVec, row_key: u8) -> BitVec{
    if row_key == 0 {return block.clone();}
    //Define BitVec Matrix to hold Results of encryption
    let mut matrix: Vec<BitVec> = vec![BitVec::from]


}

// rail_fence_decode(block: &BitVec, key: u8) -> BitVec{

// } 